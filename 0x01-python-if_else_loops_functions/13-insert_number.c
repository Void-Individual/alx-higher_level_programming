#include <stdlib.h>
#include <string.h>
#include <stdio.h>
#include "lists.h"

/**
 * insert_node - function to add a number to a node
 * @head: pointer to the head of list
 * @number: number to insert
 * Return: address of the new node, or null if it fails
 */

listint_t *insert_node(listint_t **head, int number)
{
	if (head == NULL)
		return (NULL);
}
